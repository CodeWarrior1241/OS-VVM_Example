# ================================================================================
# OS-VVM_Test .gitignore
# Committed sources: build_all.tcl, README.md, rtl/*.vhd, sim/run_sim.sh,
# sim/run_sim.tcl, sim/compile.do, sim/simulate.do, sim/full_sim_export.tcl,
# sim/tb/*.vhd
# Everything below is regenerated by the build/simulation flows.
# ================================================================================

# --- Dependencies: tracked as git submodules (deps/OsvvmLibraries,
# deps/open-logic) -- pointer commits are versioned, contents are not
# vendored. Nothing to ignore here.

# --- Vivado: project regenerated by build_all.tcl ---
OSVVM_Ethernet_Sim.xpr
OSVVM_Ethernet_Sim.cache/
OSVVM_Ethernet_Sim.gen/
OSVVM_Ethernet_Sim.hw/
OSVVM_Ethernet_Sim.ip_user_files/
OSVVM_Ethernet_Sim.runs/
OSVVM_Ethernet_Sim.sim/
OSVVM_Ethernet_Sim.srcs/
.Xil/
*.jou
*.log
*.logfile
*.str
*.pb
.gen/
.srcs/

# --- Questa simulation artifacts (created under sim/ by run_sim.sh) ---
sim/questa_lib/
sim/work/
sim/VHDL_LIBS/
sim/logs/
sim/modelsim.ini
sim/transcript
sim/*.wlf
sim/*.vstf

# OSVVM scripting/report output (Questa flow)
sim/osvvm/
sim/AXI4_build/
sim/Common_build/
sim/OsvvmTemp_Questa/
sim/index.html
sim/index.yml
sim/*.yml
sim/*.html

# --- Vivado simulator (XSim) artifacts (created under sim/ by run_sim.tcl) ---
sim/xsim_work/
xsim.dir/
*.wdb
xsim*.backup*

# --- Per-run frame captures (regenerated by every datapath test run) ---
*.pcap

# --- Rendered documentation (README.md is the source of truth) ---
README.pdf

# --- Editor / OS noise ---
*.swp
*~
.DS_Store
